# LOG

One entry per problem. Written immediately after the attempt — not at the end of the day.

Format:

```
D1-P3 | Longest Substring Without Repeating | 25min CAP HIT
Bucket: 2 (knew sliding window, couldn't handle the shrink condition)
Note: got stuck on when to move left pointer
```

Buckets:
1 — didn't recognize the pattern at all
2 — recognized the pattern, botched the implementation
3 — solved, but slow / brute-force / messy
4 — clean solve inside the cap

---
